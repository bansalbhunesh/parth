fastapi>=0.110,<1.0
uvicorn[standard]>=0.29,<1.0
pydantic>=2.6,<3.0
google-genai>=1.0,<3.0
anthropic>=0.40,<1.0
langgraph>=0.2,<2.0
numpy>=1.26,<3.0
networkx>=3.0,<4.0
pymupdf>=1.24,<2.0
pdfplumber>=0.10,<1.0
pytesseract>=0.3.10,<1.0
# Floor covers the 2026 Pillow advisory set (heap overflow / OOB read /
# DoS bundle) fixed in 12.2.0; the field previously allowed 11.3.0, which
# pip-audit flags with 7 open advisories. Public demo decodes untrusted
# image/PDF uploads, so the parser floor must guarantee patched installs.
Pillow>=12.2,<13.0
# Floor covers CVE-2024-24762/53981 AND the 2026 trio (CVE-2026-53539
# QuerystringParser quadratic DoS fixed 0.0.30; CVE-2026-40347 preamble DoS
# fixed 0.0.31; CVE-2026-24486 path traversal - non-default config we don't
# use, covered anyway). Public demo parses untrusted multipart uploads, so
# the parser floor must guarantee patched installs, not rely on resolution.
python-multipart>=0.0.31,<1.0
httpx>=0.27,<1.0
# Floor covers PYSEC-2026-3447 (fixed 83.0.0). setuptools is toolchain, not
# app code, but CI's pip-audit --local gates on whatever the runner venv
# carries (79.0.1 as of 2026-07-14), so the floor must live in requirements
# to guarantee patched installs everywhere (CI, Docker image, local setup).
setuptools>=83.0.0
openai>=1.0.0
# Pure-Python (no Pango/Cairo system deps, unlike WeasyPrint) - chosen
# specifically so the ITP PDF export doesn't add a Docker/Render build risk.
fpdf2>=2.7,<3.0
PyJWT[crypto]>=2.10,<3.0
psycopg[binary,pool]>=3.2,<4.0
redis>=6.0,<9.0
opentelemetry-api>=1.36,<2.0
opentelemetry-sdk>=1.36,<2.0
opentelemetry-exporter-otlp-proto-http>=1.36,<2.0
opentelemetry-instrumentation-fastapi>=0.57b0,<1.0
